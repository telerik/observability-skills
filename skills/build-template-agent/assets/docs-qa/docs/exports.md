# Data exports

## Export availability
CSV export download links remain available for 24 hours. After a link expires, a workspace owner must generate another export. Audit logs and export downloads have separate retention periods.

## Export contents
A CSV export contains document ID, title, owner and last-updated date. Attachments and deleted documents are excluded. The bundled demo does not generate real exports.
